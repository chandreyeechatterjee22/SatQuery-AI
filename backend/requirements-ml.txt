# Optional ML stack for captioning/VQA (RemoteCLIP). Without it the backend still runs
# and those tools report NOT_AVAILABLE. Install the CPU build of torch on Windows with:
#   pip install torch==2.14.0 torchvision==0.29.0 --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements-ml.txt
-r requirements.txt
torch==2.14.0
torchvision==0.29.0
open_clip_torch==3.3.0
huggingface_hub==2.0.0
pillow==12.3.0
psutil==7.2.2
